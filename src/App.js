import React from 'react';
import './App.css';
// eslint-disable-next-line
import { Home, About, Contact, Navigation } from './components';

const App = () => (
  <div>
    <Navigation />
  </div>
);

export default App;
